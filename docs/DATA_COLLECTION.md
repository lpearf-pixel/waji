# 数据采集规范（草案）

每个样本至少记录：

- 设备编号、品牌、型号、发动机型号和累计工时
- 工况：启动、怠速、加速、行走、回转、举升、下降、挖掘或装载
- 转速区间、负载等级和环境条件
- 传感器类型、安装位置、方向和距离
- 音频采样率、位深、通道数、开始时间和持续时间
- 维修前后状态、检查结论、更换部件和复测结果

## 文件规则

原始文件只写入一次，文件名不承载全部业务信息，业务元数据进入数据库。每个文件计算 SHA-256，用于去重和审计。

推荐首期音频格式：单声道 WAV、48 kHz、24 bit；训练前再统一转换，不直接修改原始文件。
