# Waji 开发全局约定

## 总原则

1. 先保证数据可追溯，再追求模型准确率。
2. 原始声音文件不可覆盖；清洗、切片和特征必须生成新版本。
3. 任何诊断结果必须包含模型版本、阈值版本、设备、工况和采集位置。
4. AI 输出是辅助诊断，不得表述为替代人工安全检查。
5. 业务模块之间通过明确接口交互，禁止跨模块直接修改内部数据。
6. 密钥、设备证书、数据库密码不得提交到仓库。

## API

- 统一使用 `/api/v1/` 前缀。
- 对外 ID 使用 UUID，数据库内部可使用 BigAutoField。
- 写操作必须有审计记录。
- 上传接口必须校验文件类型、大小、采样率和设备权限。

## 数据与模型

- 数据记录必须绑定 `device_id`、`machine_model`、`operating_mode`、`sensor_position` 和时间戳。
- 训练集、验证集、测试集按设备划分，避免同一台机器泄漏到不同集合。
- 模型发布必须提供离线指标、阈值依据、已知限制和回滚版本。

## 分支与提交

- 功能从 `develop` 分出，完成后通过 PR 合回 `develop`。
- `main` 只接收 `release/*` 或紧急 `hotfix/*`。
- 一个提交只处理一个清晰主题。
